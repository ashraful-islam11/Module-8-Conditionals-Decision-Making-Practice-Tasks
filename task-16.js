// todo:        topic:    Determine whether a number is even or odd using the ternary operator.

const number = 5501;

const isEvenOrIsOdd = (number % 2 === 0) ? `${number} is even number`: `${number} is odd number `;

console.log(isEvenOrIsOdd);