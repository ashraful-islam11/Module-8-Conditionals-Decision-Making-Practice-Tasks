// todo:   task:  Take a speed variable. If speed is greater than 80, print "Overspeeding", otherwise print "Normal speed".

const speed = 81;

if( speed > 80){
    console.log(`${speed} is overspeeding`);
}else{
    console.log(`${speed} Normal Speed`);
}