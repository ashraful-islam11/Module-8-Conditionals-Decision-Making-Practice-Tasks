// todo:   topic: Using isWeekend and isHoliday, print "No work today" if either one is true (use ||).

let isWeekend =true; 
let isHoliday = true;

if(isHoliday === true || isWeekend === true){
        console.log('No work today');
}
else{
    console.log(' i have lot of work today');
}