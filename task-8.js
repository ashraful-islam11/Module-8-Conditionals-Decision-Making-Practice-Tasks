// todo:   topic:Using age and hasTicket, print "Entry allowed" if age is above 18 AND the person has a ticket (use &&).

const age = 18;
const ticket =true;

if( age >= 18 && ticket === true){
    console.log('Entry allowed');
}
else{
    console.log('Entry is not allowed');
}