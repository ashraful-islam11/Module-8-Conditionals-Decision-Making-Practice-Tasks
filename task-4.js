// todo:     topic: Take a variable stock. If stock is 0, print "Out of stock" (use only if, no else).

const stock = 0;
if(stock === 0){
    console.log(`${stock} Out of stock`);
}
else{
    console.log('now i have some stock');
}